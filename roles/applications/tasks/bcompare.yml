- name: Beyond Compare
  aur:
    use: yay
    name:
      - bcompare

- name: Copy Beyond Compare config files
  copy:
    src: "{{ ansible_env.PWD }}/roles/applications/files/.config/bcompare"
    dest: "{{ ansible_env.HOME }}/.config/bcompare"
