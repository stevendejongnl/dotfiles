shadow = false;

fading = true;

fade-in-step = 0.03;

inactive-opacity = 0.95;
frame-opacity = 0.95;
menu-opacity = 0.95;
active-opacity = 0.95
opacity-rule = [
	"100:class_g = 'Google-chrome' && !focused",
	"100:class_g = 'Google-chrome' && focused",

	"90:class_g = 'jetbrains-code-with-me-guest' && !focused",
	"95:class_g = 'jetbrains-code-with-me-guest' && focused",
	"90:class_g = 'jetbrains-pycharm' && !focused",
	"95:class_g = 'jetbrains-pycharm' && focused",

	"90:class_g = 'VSCodium' && !focused",
	"95:class_g = 'VSCodium' && focused",

	"95:!focused",
	"100:focused"
];

backend = "glx";
vsync = true;

mark-wmwin-focused = true;
mark-ovredir-focused = true;

detect-client-opacity = true;
detect-transient = true;
detect-client-leader = true;

use-damage = true;
log-level = "warn";

wintypes:
{
  tooltip = { fade = true; shadow = true; opacity = 0.75; focus = true; full-shadow = false; };
  dock = { shadow = false; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1.0; }
  dropdown_menu = { opacity = 1.0; }
};
